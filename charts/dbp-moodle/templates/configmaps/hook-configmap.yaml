apiVersion: v1
kind: ConfigMap
metadata:
  name: hook-scripts
  annotations:
    "helm.sh/hook": pre-upgrade
    "helm.sh/hook-weight": "0"
    "helm.sh/hook-delete-policy": hook-succeeded
data:
  pre-upgrade-scale-down.sh: |-
{{ tpl ( .Files.Get "scripts/hook/pre-upgrade-scale-down.sh" ) . | nindent 4 }}