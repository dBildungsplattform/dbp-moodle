apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  annotations:
    "helm.sh/resource-policy": keep
  name: "{{ moodle_pvc_name }}"
  namespace: "{{ moodle_namespace }}"
spec:
  storageClassName: "nfs-client"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: "{{ moodle_pvc_size }}"